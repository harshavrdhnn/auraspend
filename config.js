// AuraSpend App Configurations
// Edit this file to add your credentials once and push to GitHub,
// and they will be automatically configured on all devices (mobile, tablet, PC).
window.AuraSpendConfig = {
    // 1. Google OAuth Client ID (for Gmail transactions scan)
    clientId: "",
    
    // 2. Cloud Sync Passcode (enter a shared phrase to link your devices)
    syncKey: "",
    
    // 3. Firebase Web Configuration JSON
    // Paste either a JSON string or copy the config object directly:
    // e.g. { apiKey: "...", databaseURL: "...", projectId: "..." }
    firebaseConfig: ""
};
